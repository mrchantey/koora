import { QueryManager } from './import'



describe('queryManager', () => {
	
	beforeEach(() => {
		
	})

	it('works', () => {
		const queryManager = new QueryManager()
		// expect(false).toBeTruthy()
	})
})